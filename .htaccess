Options -Indexes
DirectoryIndex index.php
RewriteEngine On

# 保留真实文件和目录，其他页面交给 PHP 单入口解析伪静态路由。
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

<FilesMatch "^(\.env|config\.php|.*\.sqlite|.*\.log)$">
    Require all denied
</FilesMatch>
